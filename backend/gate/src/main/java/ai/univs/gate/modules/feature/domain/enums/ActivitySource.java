package ai.univs.gate.modules.feature.domain.enums;

/** 통합 이력 행의 출처 (UG-326). MATCH = match_history(인증 시도), FEATURE = feature_history(등록·삭제). */
public enum ActivitySource {
    MATCH,
    FEATURE,
    ;
}
